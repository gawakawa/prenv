name: "GC (trigger)"

on:
  schedule:
    - cron: "0 20 * * *" # 05:00 JST daily

permissions:
  contents: read

jobs:
  gc:
    permissions:
      contents: read
      id-token: write
    # Pinned to a prenv commit SHA. Bump when adopting newer prenv changes.
    uses: gawakawa/prenv/.github/workflows/reusable-gc-prenv.yml@8dce312c1efd86c42adbc638d3580c9261ec3aa9
