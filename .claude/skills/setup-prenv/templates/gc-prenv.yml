name: "GC (trigger)"

on:
  schedule:
    - cron: "0 20 * * *" # 05:00 JST daily

permissions:
  contents: read

jobs:
  gc:
    permissions:
      contents: read
      id-token: write
    # Pinned to a prenv commit SHA. Bump when adopting newer prenv changes.
    uses: gawakawa/prenv/.github/workflows/reusable-gc-prenv.yml@feea0950ab5771874cf3ede17f57ce2345b86c0a
