name: "GC (trigger)"

on:
  schedule:
    - cron: "0 20 * * *" # 05:00 JST daily

permissions:
  contents: read

jobs:
  gc:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/reusable-gc-prenv.yml
